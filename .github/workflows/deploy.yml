name: Deploy Portfolio Production

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Instalar Cloudflared
      run: |
        # Usamos wget para asegurar la descarga del binario correcto
        wget -q https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
        chmod +x cloudflared-linux-amd64
        sudo mv cloudflared-linux-amd64 /usr/local/bin/cloudflared
        cloudflared --version

    - name: Configurar SSH
      run: |
        mkdir -p ~/.ssh
        
        # 1. Guardamos la llave privada
        echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
        chmod 600 ~/.ssh/id_ed25519
        
        # 2. Creamos la config SSH inyectando los secretos DIRECTAMENTE
        # Esto usa tu configuración de Cloudflare que ya sabemos que funciona
        echo "Host deploy-target
          HostName ssh.devortega.com
          User ${{ secrets.SSH_USER }}
          IdentityFile ~/.ssh/id_ed25519
          StrictHostKeyChecking no
          UserKnownHostsFile=/dev/null
          ProxyCommand cloudflared access ssh --hostname %h --id ${{ secrets.CF_CLIENT_ID }} --secret ${{ secrets.CF_CLIENT_SECRET }}
        " >> ~/.ssh/config

    - name: Descargar Mapa (Gran Canaria)
      run: |
        mkdir -p docker/osrm
        echo "🌍 Descargando datos de mapa (PBF) desde BBBike..."
        # Usamos el extracto PBF de Gran Canaria, que es más estable que Overpass para áreas grandes
        curl -L -o docker/osrm/gran-canaria.osm.pbf "https://download.bbbike.org/osm/bbbike/GranCanaria/GranCanaria.osm.pbf"
        ls -lh docker/osrm/

    - name: Desplegar en Servidor
      run: |
        # ASEGÚRATE QUE ESTA RUTA ES CORRECTA EN TU SERVIDOR
        TARGET_DIR="/root/portfolio"
        
        echo "🔧 Reparando DNS en el servidor..."
        # Intentamos forzar un nameserver válido para que el servidor pueda descargar imágenes de Docker
        ssh deploy-target "echo 'nameserver 1.1.1.1' > /etc/resolv.conf || true"

        echo "🧹 Limpiando espacio en disco en el servidor (Docker Prune)..."
        # Forzamos la limpieza de imágenes, contenedores y caché de construcción no utilizados
        ssh deploy-target "docker system prune -af && docker builder prune -af || true"
        
        echo "📊 Comprobando espacio tras limpieza..."
        ssh deploy-target "df -h /"

        echo "📦 Preparando y transmitiendo paquete (Direct Pipe)..."
        # Empaquetamos y enviamos directamente por SSH
        # Incluimos docker/ (con el mapa .osm) pero excluimos los archivos .osrm procesados (pesados)
        tar -cz \
          --exclude='.git' \
          --exclude='.github' \
          --exclude='.env' \
          --exclude='storage' \
          --exclude='vendor' \
          --exclude='node_modules' \
          --exclude='public/build' \
          --exclude='mdt' \
          --exclude='gtfs' \
          --exclude='docker/osrm/*.osrm*' \
          --exclude='*.deb' \
          --exclude='*.sqlite' \
          --exclude='*.log' \
          . | ssh deploy-target "
            set -e
            mkdir -p $TARGET_DIR
            
            echo '🏗️ Extrayendo código y mapa en tiempo real...'
            tar -xzf - -C $TARGET_DIR
            
            cd $TARGET_DIR
            
            # Asegurar permisos
            chmod +x deploy-compose.sh
            chmod +x init-osrm.sh
            
            echo '🐳 Ejecutando script de despliegue...'
            ./deploy-compose.sh deploy
          "